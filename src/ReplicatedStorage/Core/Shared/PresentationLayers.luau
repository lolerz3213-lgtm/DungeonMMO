--!strict
-- Shared UI presentation layers for cross-system overlays.
-- Keep the dungeon revive prompt above the existing Phase 1 defeat presentation.
return table.freeze({
    PHASE1_DEFEAT = 1000,
    DUNGEON_REVIVE = 1100,
})
